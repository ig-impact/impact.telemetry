% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/start_telemetry_logging.R
\name{start_telemetry_logging}
\alias{start_telemetry_logging}
\title{Initialize the Telemetry Logging System}
\usage{
start_telemetry_logging(log_dir = NULL, threshold = logger::INFO)
}
\arguments{
\item{log_dir}{The directory where log files will be stored. If \code{NULL}
(the default), a standard, operating-system-specific user data directory
is used, managed by \code{tools::R_user_dir}.}

\item{threshold}{The minimum log level to record (e.g., \code{logger::INFO},
\code{logger::WARN}). Defaults to \code{INFO}.}
}
\value{
Invisibly returns the full path to the log file being used for the
current session.
}
\description{
Sets up a dedicated, namespaced logger that writes structured JSON logs to a
file. This configuration is isolated and does not interfere with the global
logger or configurations from other packages. This function should typically
be called once per session.
}
\examples{
\dontrun{
# Initialize logging to the default directory
start_telemetry_logging()

# Initialize logging to a custom directory
start_telemetry_logging(log_dir = tempdir())
}
}
